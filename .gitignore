# Go
/server
*.exe
coverage.out
/tmp/

# SQLite
*.db
*.db-shm
*.db-wal

# Node / React
web/node_modules/
web/.pnpm-store/
web/dist/
!web/dist/index.html
web/.agents/
web/.claude/

prototyping/**/.agents
prototyping/**/.claude
prototyping/**/.vite
prototyping/**/dist
prototyping/**/node_modules
prototyping/**/skills-lock.json

# Env
.env.*
# ...but keep the committed template (real .env.storage stays ignored)
!.env.storage.example

# Envelope encryption KEK (never commit — decrypts all DB secrets)
/ogen-keys/
/kek/

# OS
.DS_Store
/.idea/

# Claude
CLAUDE.md
**/CLAUDE.md
/.claude/
